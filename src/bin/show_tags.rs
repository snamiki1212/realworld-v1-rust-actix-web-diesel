// extern crate conduit;

// // extern crate conduit;
// extern crate diesel;
// use conduit::app::tag::model::Tag;
// use conduit::utils::db::establish_connection;
// use diesel::prelude::*;

fn main() {
    // use conduit::schema::tags::dsl::*;

    // let connection = establish_connection();
    // let list = tags
    //     // .filter(name.eq("react"))
    //     .limit(5)
    //     .load::<Tag>(&connection)
    //     .expect("Error loading tags");

    // println!("Displaying {} list", list.len());
    // for item in list {
    //     println!("{}", item.name);
    //     println!("----------\n");
    // }
}
